---
name: vis-graph
description: "파일 의존성 그래프(dep), DB 스키마 ERD(schema), Obsidian vault 지식 그래프(vault)를 vis-network 기반 인터랙티브 HTML로 시각화. Vault 위치는 `documentation` skill 참조. Use when visualizing dependency graphs, import structure, circular dependencies, database structure, table relationships, ERD, vault knowledge graph, or page interconnections."
argument-hint: "[schema <connection-string>] [logseq [install <dir>]] [--scope=<path>] [--depth=<N>] [--exclude=<glob>]"
---
